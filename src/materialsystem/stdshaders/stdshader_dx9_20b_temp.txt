// TEMP

// This shader is broken, so only build inc files for it until fixed because im lazy
vertexlit_and_unlit_generic_vs20.fxc

// this one just crashes shader compiler, so just make inc files for it
// plus we can use the shader model 3.0 one for now anyway
Refract_ps2x.fxc